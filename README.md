Робив комміти через Git Bash =)

Git Bash commands:

git checkout david_class_restaurant

git commit -m "Added new class Restaurant"

git push / git push origin david_class_restaurant

code review

merge
